#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main(void);

void main(void) {
    int x = 10;
    int y = 20;
    int sum = (x + y);
    printf("%d\n", sum);
    int counter = 0;
    (counter = (counter + 1));
    printf("%d\n", counter);
}
