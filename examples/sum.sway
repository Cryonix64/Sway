// Sum numbers from 1 to n
func sum(n: Int) -> Int {
    var total: Int = 0
    var i: Int = 1
    
    while i <= n {
        total = total + i
        i = i + 1
    }
    
    return total
}

func main() -> Void {
    print(sum(n: 10))
}
