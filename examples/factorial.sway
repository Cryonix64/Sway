// Functions and recursion
func factorial(n: Int) -> Int {
    if n <= 1 {
        return 1
    } else {
        return n * factorial(n: n - 1)
    }
}

func main() -> Void {
    let result = factorial(n: 5)
    print(result)
}
